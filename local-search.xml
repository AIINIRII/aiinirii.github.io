<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>关于Git用法的一些总结</title>
    <link href="/2020/02/28/use-git/"/>
    <url>/2020/02/28/use-git/</url>
    
    <content type="html"><![CDATA[<p>不废话了，直接进入正题吧。</p><hr><h2 id="一、git的安装"><a href="#一、git的安装" class="headerlink" title="一、git的安装"></a>一、git的安装</h2><p>首先，在执行git有关的命令行之前，你的电脑上需要安装一个叫git的软件。</p><p>Linux：</p><p>​    命令行输入：</p><pre><code class="shell"># apt-get install git</code></pre><p>​    或者：</p><pre><code class="shell"># yum install git</code></pre><p>Windows:</p><p>​    在<a href="https://git-scm.com/download/win" target="_blank" rel="noopener">这个界面</a>选择适合你电脑的版本。</p><p>​    如果下载太慢，也可以从<a href="https://gitforwindows.org/" target="_blank" rel="noopener">这里</a>下载并安装。</p><p>MaxOS:</p><p>​    在<a href="https://git-scm.com/download/mac" target="_blank" rel="noopener">这个界面</a>进行下载及安装。</p><p>安装过程就不赘述了。</p><hr><h2 id="二、git的使用"><a href="#二、git的使用" class="headerlink" title="二、git的使用"></a>二、git的使用</h2><p>对于Linux和MacOS用户，打开终端便可直接使用git。</p><p>而对与Windows用户来说，如果你已经安装好了git，你需要做的就是在任意文件夹内右键，并选择<strong>Git Bash Here</strong>选项。</p><p>到目前为止，git的界面应该已经打开了，接下来的命令便是所有设备上通用的了。</p><hr><h2 id="三、远程仓库操作"><a href="#三、远程仓库操作" class="headerlink" title="三、远程仓库操作"></a>三、远程仓库操作</h2><h4 id="1-这里先介绍一些常用的有关远程仓库操作的git指令"><a href="#1-这里先介绍一些常用的有关远程仓库操作的git指令" class="headerlink" title="1. 这里先介绍一些常用的有关远程仓库操作的git指令"></a>1. 这里先介绍一些常用的有关远程仓库操作的git指令</h4><pre><code class="shell">   # 下载远程仓库的所有变动   $ git fetch [remote]   # 显示所有远程仓库   $ git remote -v   # 显示某个远程仓库的信息   $ git remote show [remote]   # 增加一个新的远程仓库，并命名   $ git remote add [shortname] [url]   # 取回远程仓库的变化，并与本地分支合并   $ git pull [remote] [branch]   # 上传本地指定分支到远程仓库   $ git push [remote] [branch]   # 强行推送当前分支到远程仓库，即使有冲突   $ git push [remote] --force   # 推送所有分支到远程仓库   $ git push [remote] --all   #简单查看远程---所有仓库   $ git remote    #查看单个仓库   $ git remote show [remote-branch-name]   #新建远程仓库   $ git remote add [branchname]  [url]   #修改远程仓库   $ git remote rename [oldname] [newname]   #删除远程仓库   $ git remote rm [remote-name]   #获取远程仓库数据   $ git fetch [remote-name] (获取仓库所有更新，但不自动合并当前分支)   $ git pull (获取仓库所有更新，并自动合并到当前分支)   #上传数据，如git push origin master   $ git push [remote-name] [branch]   #下载一个版本库   $ git clone &lt;版本库的网址&gt;   $ git clone &lt;版本库的网址&gt; &lt;本地目录名&gt;</code></pre><h4 id="2-创建项目并连接仓库"><a href="#2-创建项目并连接仓库" class="headerlink" title="2. 创建项目并连接仓库"></a>2. 创建项目并连接仓库</h4><ol><li><p>首先你需要有个github或者gitlab账号。</p></li><li><p>接下来，你需要进行本地账号的配置：</p><pre><code class="shell">$ git config --global user.name = &quot;你的用户名&quot;$ git config --global user.email = &quot;你的邮箱&quot; </code></pre><p>注意，其中用户名可以随便填写，但是邮箱一定要填对。</p></li><li><p>本地创建一个项目</p><p>在项目根目录下执行以下操作：</p><pre><code class="shell">$ git init$ echo &quot;# 我的git项目&quot; &gt;&gt; README.md$ echo &quot;*.log&quot; &gt;&gt; .gitignore$ git add .$ git commit -m &quot;initial commit&quot;</code></pre></li></ol><ol start="4"><li><p>接下来你需要在github或者gitlab上创建你自己的版本库。</p></li><li><p>添加远程主机地址：</p><pre><code class="shell">$ git remote add orgin &lt;版本库的网址&gt;</code></pre><p>例如：</p><pre><code class="shell">$ git remote add orgin https://github.com/jackfrued/Python-100-Days.git</code></pre></li></ol><ol start="6"><li><p>推送项目到仓库：</p><pre><code class="shell">$ git push -u origin master</code></pre></li></ol><h4 id="3-关于多人协同开发-简易版本"><a href="#3-关于多人协同开发-简易版本" class="headerlink" title="3. 关于多人协同开发(简易版本)"></a>3. 关于多人协同开发(简易版本)</h4><ol><li><p>首先需要由某个人创建好以上的仓库，并将项目上传至master分支</p></li><li><p>接下来，比如我是小A，我要加入这个项目并协同开发。那么我需要做的是以下几步：</p><ol><li><p>下载当前版本到本地：</p><pre><code class="shell">$ git init .$ git clone &lt;版本库的网址&gt;</code></pre></li><li><p>新建一个分支：</p><pre><code class="shell">$ git checkout -b &lt;new_branch_name&gt;#用git branch命令查看现在所处分支$ git branch</code></pre><p>​    注意：千万不要用master分支直接进行开发，一般master分支只作整合用。</p></li><li><p>开发完成后可将这个分支上传：</p><pre><code class="shell"> $ git add . $ git commit -m &quot;YOUR COMMIT&quot; $ git push</code></pre></li></ol></li><li><p>最后由组长进行merge工作：</p><pre><code class="shell"> $ git checkout master $ git merge &lt;new_branch_to_merge&gt; $ git commit -m $ git push</code></pre></li><li><p>所有成员更新最新版本代码库：</p><pre><code class="shell">$ git checkout master$ git pull</code></pre></li></ol>]]></content>
    
    
    
    <tags>
      
      <tag>技术文档</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>从零开始搭建v2ray+blog</title>
    <link href="/2020/02/21/Build_v2ray-blog/"/>
    <url>/2020/02/21/Build_v2ray-blog/</url>
    
    <content type="html"><![CDATA[<p>可能是因为最近疫情的原因，之前用Vultr搭的梯子被墙了。。正好，Vultr那边的余额也不是很够了，但是最近还要打美赛，亟需翻墙。于是本人咬了咬牙，狠下心来，从自己本就不多的压岁钱中抽出了一大部分购买了一个长达一年的搬瓦工VPS，并打算用这个VPS重新搭一个梯子。</p><p><img src="bwg_info.png" srcset="/img/loading.gif" alt=""></p><p>本人是一墙外小白，因此直到一个星期前，我才发现居然还有v2ray这种富强操作！然后，喜欢折腾的我便花了整整两天去查看各种YouTube上的教程。</p><p>因为VPS性能不差，我打算采取v2ray+blog的方式去榨干我的服务器性能，首先带来的便是v2ray的搭建部分。</p><h2 id="第一部分、v2ray的搭建"><a href="#第一部分、v2ray的搭建" class="headerlink" title="第一部分、v2ray的搭建"></a>第一部分、v2ray的搭建</h2><p>因为也没打算当教程写，我这儿步骤就写得稍微简单一点吧。</p><ol><li><p>首先我为自己的服务器安装了CentOS 7系统。然后在<a href="bt.cn">宝塔网站</a>成功地找到了安装宝塔系统的方法。为什么要安装宝塔系统呢？当然是为了更好地对服务器以及服务器上搭起的网站更加系统化地管理啦！</p><pre><code class="tcl">$ yum install -y wget &amp;&amp; wget -O install.sh http://download.bt.cn/install/install_6.0.sh &amp;&amp; sh install.sh</code></pre></li></ol><ol start="2"><li><p>大概等2分钟左右安装完成，然后进入宝塔面板进行MySQL+php+Nginx等的安装。</p><p><img src="bt_software.png" srcset="/img/loading.gif" alt=""></p></li></ol><ol start="3"><li>利用宝塔面板对域名进行SSL证书的签发。</li></ol><ol start="4"><li><p>回到服务器命令行那儿，输入以下指令安装v2ray </p><pre><code class="tcl"> $ bash &lt;(curl -s -L https://git.io/v2ray.sh)</code></pre></li><li><p>接下来便是一步步地设置端口啦之类的。但安装过程中有一点是需要注意的，因为我们的Nginx已经用宝塔安装好了，所以当进行到是否自动安装时<strong>一定一定要选否</strong>！！</p></li></ol><ol start="6"><li><p>现在v2ray的服务器端应该已经部署完成了，但因为Nginx的端口和v2ray不同，我们需要在（宝塔面板 –&gt; 网站    –&gt; 站点修改）中的配置文件的末尾加上以下指令</p><pre><code class="tcl">location / {        proxy_redirect off;        proxy_pass http://127.0.0.1:ababa;            #ababa处为你的端口号        proxy_http_version 1.1;        proxy_set_header Upgrade $http_upgrade;        proxy_set_header Connection &quot;upgrade&quot;;        proxy_set_header Host $http_host;    }</code></pre></li></ol><ol start="7"><li><p>灯等登凳！~~这会儿v2ray应该就可以用啦。利用以下代码生成v2ray URL</p><pre><code class="tcl">$ v2ray url</code></pre></li></ol><h2 id="第二部分、基于Hexo的blog搭建"><a href="#第二部分、基于Hexo的blog搭建" class="headerlink" title="第二部分、基于Hexo的blog搭建"></a>第二部分、基于Hexo的blog搭建</h2><p>事实上呢，在最开始的时候，我是打算用Hexo搭建，后来又打算用wordpress搭建。然后，我也不知道怎么想的，迷迷    糊糊地又用回了Hexo搭建，我想，可能是因为Hexo的主题更和我胃口吧（笑cry。</p><p>其实本质上，我并没有在我的VPS上面用到Hexo。我选择的是先在自己电脑上搭建静态网站，然后再把网站上传到VPS上。    </p><ol><li>第一步首先要做的是node.js的安装，这个可以在<a href="https://nodejs.org/en/" target="_blank" rel="noopener">node.js的官网</a>上找到相应OS版本的安装方式引导。</li></ol><ol start="2"><li><p>接着是cnpm（淘宝npm的镜像啦）的安装（符合中国国情哈哈）</p><pre><code class="tcl">$ npm install -g cnpm --registry=https://registry.npm.taobao.org</code></pre></li></ol><ol start="3"><li><p>然后安装Hexo</p><pre><code class="tcl">$ npm install -g hexo-cli</code></pre></li></ol><ol start="4"><li><p>接下来创建一个本地的网址文件夹，把它初始化</p><pre><code class="tcl">$ hexo init .</code></pre></li></ol><ol start="5"><li><p>我选择了一个比较符合我胃口的主题（这里感谢<a href="https://github.com/fluid-dev" target="_blank" rel="noopener">Fluid-dev</a>开发维护的的<a href="https://github.com/fluid-dev/hexo-theme-fluid" target="_blank" rel="noopener">Fluid</a>主题），然后一顿操作</p><pre><code class="tcl">$ git clone https://github.com/fluid-dev/hexo-theme-fluid.git themes/fluid</code></pre></li></ol><ol start="6"><li>把_config.yml中的theme改成fluid（也就是我这儿的主题文件夹名）</li></ol><ol start="7"><li><p>然后操作一下</p><pre><code class="tcl">$ hexo clean$ hexo deploy$ hexo g$ hexo s</code></pre></li></ol><ol start="8"><li>大功告成！我们可以在<a href="http://localhost:4000/" target="_blank" rel="noopener">http://localhost:4000/</a> 这个网站看我们网站的示例啦！</li></ol><ol start="9"><li><p>最后一步，把public文件夹内所有文件扔到服务器的网页文件夹下。搞定啦！部署成功！</p><p><img src="Inkedlast_step.jpg" srcset="/img/loading.gif" alt=""></p></li></ol>]]></content>
    
    
    
    <tags>
      
      <tag>技术文档</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
